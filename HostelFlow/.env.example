# Example environment variables for local development

# PostgreSQL connection string.  Replace the values with your
# database credentials and database name.  The format should be:
#   postgresql://username:password@hostname:port/database_name
DATABASE_URL=postgresql://postgres:password@localhost:5432/hostel_db

# The port the Express server will listen on when running
# `npm start`.  Defaults to 5000 if not specified.
PORT=5000

# Secret used by session middleware.  Although the simplified
# authentication in this repository does not create sessions, you
# may wish to add your own authentication mechanism.  Provide a
# sufficiently long random value here if you enable sessions.
SESSION_SECRET=change-me